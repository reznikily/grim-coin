# 🪙 GrimCoin - Учебная децентрализованная платёжная сеть

GrimCoin — это упрощённая децентрализованная платёжная сеть, разработанная для изучения принципов работы распределённых систем и блокчейн-технологий в локальной сети.

## 📋 Архитектура системы

Система состоит из трёх основных компонентов:

### 🏛️ Controller (Контроллер)
- **Порт P1 (8000)**: WebSocket сервер для технических сообщений от кошельков
- **Порт P2 (8080)**: WebSocket сервер для веб-клиента наблюдателя
- Управляет учётом подключённых узлов и их состояниями
- Координирует сетевые обновления

### 💼 eWallet (Электронный кошелёк)
- Подключается к контроллеру через WebSocket
- Хранит локальный реестр (ledger) с балансами всех участников
- Персистентное хранение состояния в JSON файлах
- Каждый кошелёк стартует с балансом 10 монет

### 🌐 Web Observer (Веб-наблюдатель)
- HTML/JavaScript интерфейс для преподавателя
- Отображает состояние сети в реальном времени
- Показывает балансы, статусы узлов и метрики сети
- Не требует внешних зависимостей

## 🚀 Быстрый старт

### Установка зависимостей

```bash
make deps
```

### Сборка проекта

```bash
make build
```

Или для macOS ARM64 (согласно пользовательским настройкам):

```bash
make build-darwin-arm64
```

### Демонстрация системы

Запустите полную демонстрацию с контроллером и тремя кошельками:

```bash
make demo
```

Затем откройте `web/observer.html` в браузере для наблюдения за состоянием сети.

### Ручной запуск компонентов

**1. Запуск контроллера:**
```bash
make run-controller
# или
go run ./cmd/controller
```

**2. Запуск кошельков в отдельных терминалах:**
```bash
# Терминал 2
make run-wallet-1
# или
go run ./cmd/wallet -config configs/wallet-1.yaml

# Терминал 3
make run-wallet-2

# Терминал 4
make run-wallet-3
```

**3. Открытие веб-интерфейса:**
Откройте файл `web/observer.html` в любом современном браузере.

## 📁 Структура проекта

```
grim-coin/
├── cmd/
│   ├── controller/          # Исполняемый файл контроллера
│   └── wallet/              # Исполняемый файл кошелька
├── internal/
│   ├── controller/          # Логика контроллера
│   ├── wallet/              # Логика кошелька
│   ├── protocol/            # Протокол сообщений
│   └── store/               # Система хранения данных
├── configs/
│   ├── wallet-1.yaml        # Конфиг кошелька Ильи
│   ├── wallet-2.yaml        # Конфиг кошелька Анны
│   └── wallet-3.yaml        # Конфиг кошелька Дмитрия
├── data/                    # Файлы состояний кошельков (создаются автоматически)
├── web/
│   └── observer.html        # Веб-интерфейс наблюдателя
├── bin/                     # Собранные бинарные файлы
└── Makefile                 # Команды сборки и запуска
```

## 🔧 Конфигурация

### Пример конфигурации кошелька (configs/wallet-1.yaml):

```yaml
id: 1                                    # Уникальный ID кошелька
name: "Илья"                             # Отображаемое имя
ip: "192.168.1.2"                        # IP адрес кошелька
listen_p3: "0.0.0.0:9000"                # Порт для P2P соединений (этап 2)
controller_ws: "ws://localhost:8000/ws"   # URL контроллера
peers:                                   # Список других кошельков (этап 2)
  - "ws://192.168.1.121:9000/ws"
  - "ws://192.168.1.15:9000/ws"
```

## 📨 Протокол сообщений

### Общая структура (JSON Envelope):
```json
{
  "type": "message_type",
  "sender_id": 1,
  "version": 0,
  "tx_id": "optional_transaction_id",
  "payload": { ... }
}
```

### Типы сообщений этапа 1:
- **`hello`** - Подключение кошелька к контроллеру
- **`state_update`** - Обновление локального состояния кошелька
- **`network_state`** - Состояние всей сети (для веб-клиента)
- **`ack`** - Подтверждение получения сообщения
- **`error`** - Сообщение об ошибке

## 🧪 Тестовые сценарии

### ✅ Старт сети
Все кошельки подключаются, контроллер выводит таблицу, веб-клиент отображает балансы 10/10/10.

### ✅ Перезапуск узла
Остановка и запуск кошелька — он восстанавливает баланс и версию из локального хранилища.

### 📊 Мониторинг состояния
Веб-интерфейс показывает:
- Общее количество узлов
- Количество онлайн узлов
- Суммарный баланс сети
- Текущую версию сети
- Детальную таблицу с информацией по каждому узлу

## 🛠️ Команды Makefile

### Сборка:
- `make build` - Сборка всех компонентов
- `make build-controller` - Сборка только контроллера
- `make build-wallet` - Сборка только кошелька
- `make build-darwin-arm64` - Сборка для macOS ARM64

### Запуск:
- `make run-controller` - Запуск контроллера
- `make run-wallet-N` - Запуск конкретного кошелька (N=1,2,3)
- `make demo` - Запуск полной демонстрации

### Разработка:
- `make deps` - Установка зависимостей
- `make test` - Запуск тестов
- `make lint` - Линтинг кода
- `make fmt` - Форматирование кода

### Обслуживание:
- `make clean` - Очистка артефактов сборки и данных
- `make status` - Проверка состояния системы
- `make stop` - Остановка всех процессов

## 🔍 Диагностика и логирование

Все компоненты ведут подробные консольные логи с временными метками:
- Подключения и отключения узлов
- Отправка и получение сообщений
- Ошибки и предупреждения

Пример лога кошелька:
```
2024/09/26 15:30:15 client.go:123: Starting wallet 1 (Илья)
2024/09/26 15:30:15 client.go:145: Successfully connected to controller
2024/09/26 15:30:15 client.go:178: Sent hello: ID=1, Name=Илья, Balance=10, Version=0
```

## 🚧 Статус реализации

### ✅ Этап 1 (Завершён)
- ✅ Сетевая инфраструктура и WebSocket серверы
- ✅ Протокол сообщений JSON
- ✅ Контроллер с учётом узлов
- ✅ Кошельки с персистентным хранением
- ✅ Веб-интерфейс наблюдателя
- ✅ Конфигурационные файлы
- ✅ Система сборки и документация

### 🔄 Следующие этапы
- **Этап 2**: Транзакции, P2P соединения, эксклюзивная блокировка
- **Этап 3**: Обнаружение десинхронизации, восстановление, нагрузочное тестирование

## 🤝 Использование

Этот проект предназначен для образовательных целей и демонстрации принципов работы децентрализованных систем. Используется в рамках лабораторных работ по изучению сетевых технологий и распределённых систем.

## 📄 Лицензия

MIT License - см. детали в файле LICENSE.
